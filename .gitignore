*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build/
/captures/
.externalNativeBuild/
.cxx/
local.properties
/app/build/
/app/release/

# Debug signing key: kept off git deliberately, reconstructed by CI from a repo secret
# (DEBUG_KEYSTORE_BASE64) — see .github/workflows/build.yml
/keystore/

# Locally-downloaded build artifacts, not source
/dist/
